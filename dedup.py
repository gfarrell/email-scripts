def cmd_dedup(mailbox, source, target, dest):
    """find and remove duplicate emails"""
    # get all emails in the target
    # for each message
    # get message-id $id
    # find all messages with $id in source
    # set original uid $og as first id in source
    # find all messages with $id in target
    # for all messages in target with uid which is not $og
    # move to dest folder
    pass
